---

- name: 'copy the stack file'
  template:
    src: stack.yml.jinja2
    dest: /tmp/resources/traefik.stack.yml
    force: true
    trim_blocks: false
    lstrip_blocks: false
    mode: 0644
  register: deploy_stack
