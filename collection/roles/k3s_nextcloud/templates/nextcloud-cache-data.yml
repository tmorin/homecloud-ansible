---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-cache-data
  namespace: "{{ k3s_nextcloud_namespace }}"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "{{ k3s_nextcloud_cache_storage_class }}"
  resources:
    requests:
      storage: "{{ k3s_nextcloud_cache_storage_size }}"
---