---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: libvirt
platforms:
  - name: instance
    box: debian/buster64
lint: |
  set -ex
  #ansible-lint
provisioner:
  name: ansible
verifier:
  name: ansible
