---

- hosts: k3s_srv[0]
  tasks:
    - name: Verify k3s deployments
      include_tasks: ../tasks/verify-{{ k3s_deployment }}.yml
      loop: "{{ k3s_deployments | default([]) }}"
      loop_control:
        loop_var: k3s_deployment
