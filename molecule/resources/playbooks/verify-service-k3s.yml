---

- hosts: k3s
  tasks:
    - name: Get K3S info
      command: which k3s
      ignore_errors: yes
      register: k3s_info
      changed_when: false
    - name: Verify K3S installation
      assert:
        quiet: true
        that:
          - not k3s_info.failed
