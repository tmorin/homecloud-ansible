---

all:
  children:
    # CEPH
    ceph_mon:
      hosts:
        c1-ceph-n1:
    ceph_mgr:
      hosts:
        c1-ceph-n1:
    ceph_osd:
      hosts:
        c1-ceph-n1:
    ceph:
      children:
        ceph_mon: { }
        ceph_mgr: { }
        ceph_osd: { }
