---

- hosts: syncthing
  pre_tasks:
    - import_tasks: tasks/prepare-resources.yml
  tasks:
    - include_role: name=cluster_node
    - include_role: name=service_syncthing
  post_tasks:
    - import_tasks: tasks/clean-resources.yml
