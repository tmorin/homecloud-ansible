---

- name: 'check parameters'
  assert:
    that:
      - nas_mount_what | length > 0
      - nas_syncthing_gui_password | length > 0

- import_tasks: install-nas-mount.yml
- import_tasks: add-user-nas.yml
- import_tasks: install-samba.yml
- import_tasks: install-syncthing.yml
- import_tasks: configure-syncthing.yml
