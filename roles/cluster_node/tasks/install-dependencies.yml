---

- name: 'install packages'
  become: yes
  package:
    name: ntp,python3,python3-pip
    state: present
    force_apt_get: "yes"
  register: upgrade_system

- name: 'install python modules'
  pip:
    name:
      - setuptools
    extra_args: --user
  register: upgrade_system
