---

# The ceph should not be overridden.
ceph_release: octopus

# The IP of the main monitor should not be overridden.
ceph_monitor_ip: "{{ hostvars[groups['ceph_mon'][0]][homecloud_network_interface].ipv4.address }}"
