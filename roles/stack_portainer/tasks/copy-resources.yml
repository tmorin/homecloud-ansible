---

- name: 'copy the stack file'
  template:
    src: stack.yml.jinja2
    dest: /tmp/resources/portainer.stack.yml
    force: true
    trim_blocks: no
    lstrip_blocks: no
  register: deploy_stack
